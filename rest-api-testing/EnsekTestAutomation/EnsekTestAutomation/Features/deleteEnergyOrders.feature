Feature: Delete energy orders
    
    The API will allow a specified order to be deleted
    
    # Pending Impl - the API for delete was throwing HTTP 500's
    # Tests would call the delete endpoint then call the GET orders endpoint as part of the test assertions